export const EMAIL = "hello@studiopresenttense.com";

// Font size lines are measured at before being scaled to fill the viewport.
export const REFERENCE_FONT_SIZE = 100;

// How often the hero image cycles.
export const HERO_INTERVAL_MS = 1500;

// Fraction of the hero's available width the email scales to fill. Tweak to
// taste: 1 fills the full padded width, lower values leave room on the right.
export const EMAIL_FILL_MOBILE = 0.75;
export const EMAIL_FILL_DESKTOP = 1;

// Tailwind `sm` breakpoint (px, see globals.css); at/above it the desktop email
// is shown, matching the `sm:` layout swap.
export const SM_BREAKPOINT = 641;
